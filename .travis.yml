language: ruby

rvm:
  - 2.7.5
  - 3.1.1

# script, expressed as an array, is necessary for 'bundle exec coveralls push' to work locally
script:
  - bundle exec rake

cache: bundler

notifications:
  email: false
